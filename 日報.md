# 日報運用ルール

- このファイルは Codex の自己改善ログとして使う
- 単なる作業報告ではなく、次回の Codex が良く動くための記録を書く
- 追記式で使う
- 意味のある作業単位ごとに書く
- 短く、具体的に書く
- テンプレ：
```md
---
## YYYY-MM-DD
- 対象: web / extension / supabase / infra / prompt / docs / other
- 変更種別: 実装 / 調査 / デバッグ / テスト / デプロイ / 運用

### 今日の作業
- 
### 分かったこと
- 
### 次回への教訓
- 
```

---
## 2026-04-11
- 対象: web / supabase / infra / docs / other
- 変更種別: 実装 / デバッグ / テスト / デプロイ / 運用

### 今日の作業
- LINE公式アカウント `@295bwysj` をブラウザ操作で新規作成し、Messaging API を有効化した
- LINE連携コード、Webhook、push 配信 worker、音声変換、GCS 配信、配信履歴管理まで一式実装した
- `profiles` 拡張と `line_link_codes` / `line_delivery_jobs` を含む Supabase migration を追加し、反映した
- Reading 画面から LINE 設定を外し、右上ユーザーメニューから開く `/settings` に移した
- 新しい連携コードを送ったときに、既存の LINE 連携を上書きできるように変更した
- `typecheck` / `lint` / `build:web` を通し、Vercel production へ複数回デプロイした

### 分かったこと
- LINE Developers 側は Webhook URL を保存するだけでは不十分で、`Webhookの利用` を明示的に ON にしないとイベントが飛ばない
- Vercel の production env に改行付き値や別プロジェクト ID が残っていると、Cloud Tasks で気付きにくい 404 / 403 になる
- 既存の `GOOGLE_APPLICATION_CREDENTIALS_JSON` が別 GCP プロジェクトの service account でも、必要な IAM を付ければ当座は動かせる
- typed routes 有効時は `Link href=\"/settings\"` のような追加ルートで型エラーが出ることがある
- LINE 配信の最終確認は、DB 上の `line_delivery_jobs.status=completed` と `line_request_id` の両方を見ると早い

### 次回への教訓
- LINE 連携の E2E では、先に `Webhookの利用`、Webhook URL、署名疎通の3点を確認してからアプリ側を疑う
- Vercel env は `vercel env pull` で実値を確認し、GCP project/location に改行が混じっていないか必ず見る
- Cloud Tasks / GCS を跨ぐ実装では、service account の実体を先に特定して IAM をまとめて整える方が手戻りが少ない
- 設定 UI を追加するときは、機能ページへ直置きせずユーザーメニュー配下へ寄せると後から整理しやすい
- コミット粒度は「機能実装」と「UI導線変更」を分けると後で追いやすい

---
## 2026-04-11
- 対象: web / infra / docs
- 変更種別: 調査 / 実装 / テスト / 運用

### 今日の作業
- Cloud Tasks 404 (`Queue does not exist`) の原因を、Vercel production env と GCP 実キュー一覧の照合で特定した
- `gen-lang-client-0926290743/us-west1` に不足していた `reading-generate` キューを作成した
- `apps/web/lib/app-env.ts` でキュー名専用の正規化を追加し、末尾 `\n` や複数行混入を除去するようにした
- `apps/web/lib/cloud-tasks.ts` に起動時キューヘルスチェック（`tasks.queues.get`）を追加し、未作成キューを早期ログ検知できるようにした
- deploy runbook に Cloud Tasks キュー作成手順を追記し、`lib/app-env.test.ts` で正規化のテストを固定化した

### 分かったこと
- 404 の切り分けは、アプリコードだけでなく「本番 env の実値」「同じ project/location のキュー一覧」を同時確認するのが最短
- 環境変数の末尾 `\n` 混入は一見無害でも、識別子系（キュー名/ID）では事故率が高い

### 次回への教訓
- Cloud Tasks 障害時は最初に `vercel env pull` と `gcloud tasks queues list` をセットで実行する
- キュー名やIDは専用正規化関数を通し、一般文字列の正規化に依存しない
- runbook には「必要リソースを作るコマンド」を必ず明記し、env 設定だけで完了扱いにしない
